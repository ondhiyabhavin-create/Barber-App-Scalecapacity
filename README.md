# Barber-App-Scalecapacity

BarberOS — Next.js + Supabase multi-tenant barbershop SaaS. See `apps/web` and `supabase/migrations`.

Local setup: copy `apps/web/.env.example` to `apps/web/.env.local` and set your Supabase URL and anon/publishable key.
